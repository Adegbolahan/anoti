# anoti

Before starting any work in this project, read `GROUNDING.yaml` — the
append-only record of established facts, their confidence levels, and open
questions. Ground your answers in it instead of re-deriving; append new
discoveries there following the conventions in its header (add both a
`discoveries` entry and an `index` row).
